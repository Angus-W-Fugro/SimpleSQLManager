<Window x:Class="SQLManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SQLManager"
        mc:Ignorable="d"
        Title="SQLManager"
        Height="450"
        Width="800"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        d:DataContext="{d:DesignInstance Type=local:MainWindowModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="12">
            <ui:SimpleStackPanel Spacing="12">
                <TextBox Text="{Binding ServerName}" Width="300" />
                <Button Content="Connect" Command="{Binding ConnectCommand}" />
                <ListBox ItemsSource="{Binding DatabaseNames}" SelectedItem="{Binding SelectedDatabaseName}" />
            </ui:SimpleStackPanel>
        </Grid>

        <Grid Grid.Column="1" Margin="12">
            <ui:SimpleStackPanel Spacing="12">
                <TextBox Text="{Binding SQLText}" />
                <Button Content="Execute" Command="{Binding ExecuteSqlCommand}" />
                <TextBlock Text="{Binding SQLResponse}" />
            </ui:SimpleStackPanel>
        </Grid>
    </Grid>
</Window>
